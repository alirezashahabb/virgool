namespace NodeJS {
    interface ProcessEnv {
        PORT: string;
        //DataBase
        DB_HOST: string;
        DB_PORT: number;
        DB_USERNAME: string;
        DB_PASSWORD: string;
        DB_NAME: string;

    
    }
}
